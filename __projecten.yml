---
---
{% for p in site.projecten %}- titel: "{{ p.titel }}"
  type: {{ p.type }}
  status: {{ p.status }}
  domein: {{ p.domein }}
  opdrachtgever: {{ p.opdrachtgever }}
  aanpak: {{ p.aanpak }}
  rol: {{ p.rol }}
  omschrijving: "{{ p.omschrijving }}"
  begin: {{ p.begin }}
  einde: {{ p.einde }}
  duur: {{ p.duur }}
  sort-jaar: {{ p.sort-jaar }}
  standplaats: {{ p.standplaats }}
  vaardigheden: {{ p.vaardigheden }}
  url: {{ p.url }}
  tags: {{ p.tags }}
{% endfor %}