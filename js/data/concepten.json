---
---
{
    "concepten": {
        {% for concept in site.concepten %}
            "{{ concept.title | downcase }}": "{{ site.url }}{{ concept.url }}"{% if forloop.last == false %},{% endif %}
        {% endfor %}
    }
}